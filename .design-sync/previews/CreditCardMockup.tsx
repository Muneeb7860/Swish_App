import { CreditCardMockup } from "frontend-b2b";

// Takes no props — the card face (chip, hologram, masked PAN, holder line) is
// fixed brand furniture used inside CheckoutPanel's payment step. One story is
// the whole component; a second export would be an identical cell.

export const Default = () => <CreditCardMockup />;
