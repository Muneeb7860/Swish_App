# Implementation Plan — Week of Jul 13–17, 2026

**Theme:** land in-flight work, close two known correctness/security gaps, start FR-01 retailer portal.
**Constraints:** branch flow is `mac-machine → develop` only (never PR to master, no new feature branches). AI infra stays local/free-tier only. `JAVA_HOME` = microsoft-17 JDK for all backend builds.

## Goals
1. develop carries all of last week's fixes (PR #146) plus the pending WIP, cleanly committed.
2. Controller-layer authz (IDOR guards) has real MockMvc test coverage.
3. Dev/E2E checkout works end-to-end (dark-store seeding), unlocking the idempotency E2E test.
4. FR-01 retailer portal underway with scoped design and first vertical slice.

## Monday — land everything in flight
- **Merge [PR #146](https://github.com/Muneeb7860/Swish_App/pull/146)** (green + mergeable as of Sat). Confirm develop CI green afterwards.
- **Commit the pending WIP as separate commits** (currently sitting dirty in the tree):
  - `docs:` service_decoupling_plan.md + ROADMAP Epic-6 checkbox — **fix the `file:///Users/muneeb/Documents/...` absolute link to a relative `./docs/...` link** (old path is dead since the repo moved to `~/GitHub`).
  - `test(arch):` HexagonalArchitectureTest additions (+29 lines) — run the arch suite before committing.
  - `feat(pricing):` competitor-pricing-server rework (index.js 216 lines + package.json + lock) — boot the server (port 8089), hit one endpoint before committing.
- **MFA decision:** commit `cd63716` is pinned by local tag `wip/mfa-otp-rescue` (it was dangling — GC risk). Decide: cherry-pick onto `mac-machine` (flag `swish.auth.mfa.enforced` stays `false`) or park it by pushing the tag. Don't leave it local-only another week.
- **Post-move rebuilds:** recreate `homelab-ai-governance/.venv` (Python 3.14, includes `instructor`), verify `pytest` collects; `mvn package` on demand per module.
- Effort: ~1 day total.

## Tuesday–Wednesday — close the two known gaps
- **Controller authz MockMvc tests** (integration tests currently bypass controllers, so IDOR guards are untested):
  - Matrix per endpoint: no token → 401; own resource → 200; other customer's resource → 403; admin → 200.
  - Start with `transaction/adapter/in/web/OrderController` (GET/POST/refund), then sweep other controllers taking `customerId`/owner-scoped params.
  - Use MockMvc with the JWT security filter chain active (not `@WebMvcTest` with security stripped).
- **Dark-store/catalog seeding for dev profile:** checkout currently 400s with "Dark Store not found: Central Store", which silently skips the idempotency E2E test. Seed Central Store + a catalog item (e.g. BREAD-001) via dev-profile Flyway seed or the existing DevAdminInitializer. ⚠️ Schema-drift rule: any entity/column change ships with a Flyway migration — no relying on local ddl-auto.
  - Success: POST /api/v1/orders returns 201 in E2E; idempotency spec runs instead of skipping; 62/62 stays green with one more effective test.
- Effort: ~1.5 days combined.

## Wednesday–Friday — FR-01 retailer portal (roadmap R2 follow-on)
- Wed: scope + one-page design (personas, entities, endpoints, which MFE hosts it — likely frontend-b2b, port 5002).
- Thu–Fri: first vertical slice behind a feature flag (one entity end-to-end: migration → port/adapter → controller with authz tests from day one → MFE screen). E2E smoke at the end.
- Effort: ~2 days for the slice; carry-over into next week is expected and fine.

## Friday PM — wrap
- Full E2E matrix run; update ROADMAP checkboxes; draft next week's plan.
- Optional cleanup: delete/archive the still-iCloud-synced `~/Documents/GitHub/` backup folders.

## Risks & notes
- Backend suite is flaky under load — run heavy suites serially, not alongside builds.
- CI "Enforce Standards Gates" validates conventional commits across the whole PR range — one bad message blocks the PR (`security:` not `sec:`).
- Cypress stabilization is an Epic-6 backlog item — timebox any flake-chasing to 30 min, then file it.

## Out of scope this week
- PickerScreen.tsx modular refactor (deferred until integration testing completes).
- R5 wiring of homelab-ai-governance into the platform (stretch only if the week runs clean; Phase-5 MinHash gap still open).
- Epic-6 zero-trust mesh / SPIFFE / secrets-rotation items.
