# Handover — AI Governance Hardening & Red-Team Tooling (as of 2026-07-22)

**Branch:** `mac-machine` · **Read this before touching `homelab-ai-governance/`, `backend/domain/agent/`, or `agentic-redteam/`.**

## ⚠️ Read this first: two writers, one branch

This branch has had **two Claude sessions committing concurrently** for several
days (evidenced by interleaved unrelated commits — e.g. an "Arabic language
support" commit landed mid-thread from a different session). This caused at
least one garbled-commit incident already. If you are picking this up:

1. `git fetch origin && git log --oneline -20` **before** starting — do not
   trust local state as current.
2. Check `git stash list` — there is one stash parked (see below), do not drop
   it without checking what's in it.
3. If another session is active on this repo, coordinate or wait — do not
   commit on top of an unknown working tree.

## 🔴 CI is currently RED — fix this first

**Tip of branch (`a9ad29b`) fails CI** on the `AI Governance Quality Gate
(Python)` job. Root cause is diagnosed, not yet fixed:

```
[✅ PASS] pii_leakage          15/15
[❌ FAIL] prompt_injection     22/25
[❌ FAIL] indirect_injection   1/4
[❌ FAIL] jailbreak            19/20
[✅ PASS] code_safety          15/15
[❌ FAIL] schema_compliance    3/15   ← near-total wipeout
[✅ PASS] action_level         5/5
[❌ FAIL] clean_queries        0/10   ← total wipeout
[✅ PASS] multi_turn           2/2
[✅ PASS] centroid_probes      3/3
[❌ FAIL] crypto_probes        4/5
```

**Diagnosis:** total red-team requests across all categories = **124**, and
the governance service's hourly rate limit
(`homelab-ai-governance/config/routing_config.yaml` →
`budget.hourly_request_limit: 100`) caps at **100/hr**. The `schema_compliance`
(3/15) and `clean_queries` (0/10) categories run last/near-last in the suite
and get wholesale-rejected by `RateLimiter.is_allowed()`
(`src/governance/audit.py:253`) once the cap is hit — **this is a test-harness
artifact, not necessarily 8 real guardrail regressions.** I diagnosed this
exact failure signature once before in this thread (same 0/10 and 3/15
pattern) when the suite was smaller; it has now recurred because the suite
grew (new `indirect_injection`, `crypto_probes` categories were added by the
other session) past the rate cap.

**Recommended fix (not yet applied — do this first):**
Add a CI-only bypass to `RateLimiter.is_allowed()`, mirroring the existing
`GOVERNANCE_ALLOW_MOCK_FALLBACK` honesty-gate pattern already used elsewhere
in this codebase (see `src/governance/agents/_mock.py`):

```python
# in RateLimiter.is_allowed(), near the top:
if os.environ.get("GOVERNANCE_ALLOW_MOCK_FALLBACK", "").lower() in ("1", "true"):
    return True
```

The CI job already sets `GOVERNANCE_ALLOW_MOCK_FALLBACK=1` when starting the
server (see `.github/workflows/ci.yml`, governance job), so this requires no
new CI config — just the code change. **After applying, re-run
`python scripts/redteam.py --ci` locally against a running server and confirm
the previously-wiped categories now report real pass/fail numbers** — only
then are the remaining 22/25, 19/20, 1/4, 4/5 results trustworthy enough to
triage as genuine guardrail gaps (or not).

Do **not** just raise the limit number — that masks the same class of bug
recurring as the suite keeps growing. The env-gated bypass is scoped to
CI/test explicitly.

## 🟡 Uncommitted work in the working tree — needs review, not blind commit

As of this handover, `git status` shows:
- 4 modified `agentic-redteam.egg-info/*` files — **auto-generated build
  artifacts** (from `pip install -e`), safe to discard (`git checkout --`) or
  add to `.gitignore`; not real changes.
- Modified: `agentic-redteam/agentic_redteam/cli.py`, `pyproject.toml`,
  `redteam_results.json` — real, unreviewed changes from the other session.
- New untracked: `sarif_exporter.py`, `scoring.py`, `swishos/` (a mini SDK:
  `sdk.py`, `exceptions.py`, `__init__.py`), `sample.sarif`,
  `tests/test_scoring.py`, `tests/test_sdk.py` — **none of this has been
  reviewed or tested by me.** Read it before trusting it.

## 🟡 One stash parked — decision still pending

```
stash@{0}: On mac-machine: uncommitted repo-wide reformat (parallel session, 2026-07-19) — reversible, ask before dropping
```

~50 files of repo-wide auto-formatting from the other session, stashed
(not discarded) on 2026-07-19 pending a keep/drop decision from the user.
Still unresolved. Do not `git stash drop` without checking with the user —
run `git stash show -p stash@{0} | head -100` to see what it contains first.

---

## ✅ What's done, verified, and safe to build on (commits `d8b553d` → `d009822`)

All of the following are **merged to `mac-machine`, individually verified
(tests + live checks), and were CI-green at the time they landed** (before the
suite grew past the rate cap):

| Commit | What | Verification |
|---|---|---|
| `d8b553d` | AL-05 (refund-bypass) risk elevation fix | Unit + live; unblocked a red CI at the time |
| `9630307`+`0fdf84b` | **Security fix**: Java `PythonGovernanceAdapter` now honors Phase-4 "shed" (503) instead of silently falling back to an *ungoverned* cloud LLM on a governance outage | Adapter unit tests (12/12) + gateway tests (11/11) |
| `15371d9` | Live cross-process proof of the above: real Java adapter → real HTTP → live degraded Python service → real 503 shed observed | Manual live run, log-captured |
| `30a1f74` | Ruff lint cleanup + fixed an F811 test-name collision that was silently shadowing a test | 236 passed at the time; caught that the "obvious" fix (rename) would have un-shadowed a *flaky* test — removed the stale duplicate instead |
| `307bc4c` | Unified 3 duplicate red-team payload/runner copies into one canonical `agentic-redteam` package; `scripts/redteam.py` is now a thin shim | CI green at merge; package runs both as Swish's CI gate and standalone for demos |
| `d009822` (other session) | AST-based SQL validator, elevated destructive-command severity | Not independently reviewed by me |

**The Phase-4/503 security fix (`9630307`/`15371d9`) is the highest-value item
in this list and is solid** — closes a real "governed request silently
answered by an ungoverned model during an outage" hole. Don't second-guess
that one without cause; it has both unit and live cross-process verification.

---

## 📋 Roadmap — next, in priority order

1. **Fix the rate-limiter CI artifact** (above) — blocks everything else from
   being trustworthy. Small, well-specified, ~15 min.
2. **Re-triage the red-team suite** once (1) is fixed. Only then do the
   `prompt_injection 22/25`, `indirect_injection 1/4`, `jailbreak 19/20`,
   `crypto_probes 4/5` numbers mean anything — some or all may resolve once
   rate-limiting isn't confounding them. For whatever's still failing after
   that, apply the same pattern used throughout this thread: read the actual
   failing payload + response, add a targeted guardrail pattern, add a
   regression test with both the attack payload AND a "must not over-block"
   clean-query counterpart.
3. **Review the uncommitted `agentic-redteam` additions** (sarif export,
   scoring, `swishos` SDK) before trusting or committing them — unreviewed as
   of this handover.
4. **Resolve the stash** — get a keep/drop decision on the parked repo-wide
   reformat (`stash@{0}`), it's been sitting for 3 days.
5. **Decide on MVC/JWT-layer coverage.** Everything verified so far proves the
   `PythonGovernanceAdapter` ↔ Python service boundary. The Spring MVC
   controllers + JWT auth *in front of* that adapter have not been
   integration-tested end-to-end (booting the full stack needs Postgres/
   Kafka/Keycloak/Temporal — a bigger, separate session). Low risk since the
   fixed bug doesn't live in that layer, but it's a real gap if full
   confidence is wanted.
6. **Phase 5 items (still correctly deferred, unchanged):** multi-window
   burn-rate alerts, WORM audit sink, async advisory-guardrail split. Don't
   build these speculatively — see `homelab-ai-governance/docs/GOVERNANCE_SPEC.md`
   §5, gated on evidence (an actual SLO breach or a sub-second consumer
   showing up).

---

## 🛠️ Environment bootstrap (cold start)

- **Repo:** `~/swish-bench` (the only clone on this Mac; not iCloud-synced —
  see memory `feedback_never_depend_on_icloud`). Working branch `mac-machine`.
- **Python governance service:** `homelab-ai-governance/`, venv at
  `.venv/bin/python`. Start with:
  ```bash
  cd homelab-ai-governance
  OTEL_SDK_DISABLED=true GOVERNANCE_ALLOW_MOCK_FALLBACK=1 bash dev/start_governance_server.sh
  ```
- **Ollama models live on the T7 SSD**, not the internal disk:
  `~/.ollama` → `/Volumes/T7/ollama_models`. **Plug in the T7 before running
  anything that needs live inference** — cold loads off the wrong disk were
  measured at 110s vs 7.5s on the T7 (see memory
  `reference_ollama_models_on_t7_ssd`).
- **Java backend:** `JAVA_HOME=/Library/Java/JavaVirtualMachines/microsoft-17.jdk/Contents/Home`,
  build with `mvn -f backend/pom.xml <goal>` (it's a standalone Maven project,
  not in the root reactor).
- **Red-team tool:** `agentic-redteam/` (repo root) is `pip install -e`-able
  and runs standalone against any `/govern`-shaped endpoint, or via the
  `scripts/redteam.py` shim for Swish's own CI gate.
- **Full context:** read
  `~/.claude/projects/-Users-muneeb-swish-bench/memory/MEMORY.md` and
  `homelab-ai-governance/docs/GOVERNANCE_SPEC.md` (canonical spec, kept
  current via doc-as-code — any PR changing enforcement behavior must update
  it in the same PR).

---

*Written 2026-07-22. If you're a fresh session picking this up: `git fetch`,
read this file top to bottom, then start at item 1 of the roadmap.*
