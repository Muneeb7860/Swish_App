import type React from "react";

/**
 * Multi-stage order progress timeline component.
 * Displays ordered progression with active, completed, and failed states.
 */
export const OrderTimeline: React.FC;
