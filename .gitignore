# ─── Node / Frontend ─────────────────────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
.cache/
.parcel-cache/
.turbo/
.next/
.nuxt/
.output/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# ─── Java / Maven build artifacts ────────────────────────────────────────────
target/
**/target/
*.class
*.jar
*.war
*.ear
*.nar

# ─── Embedded Maven runtime (use system Maven or mvnw wrapper instead) ────────
apache-maven-*/
**/apache-maven-*/
maven.zip
**/maven.zip
mvn-*.zip

# ─── Spring Boot ─────────────────────────────────────────────────────────────
*.orig

# ─── Docker ──────────────────────────────────────────────────────────────────
.docker/

# ─── Environment / secrets ───────────────────────────────────────────────────
.env
.env.*
.env.development
nginx/ssl/
.env.staging
.env.production
!.env.example
*.pem
*.key
*.p12
*.keystore
*.jks

# ─── IDE / Editor ────────────────────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.iml
*.iws
*.ipr
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
.project
.classpath
.settings/

# ─── OS junk ─────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini
# macOS Finder duplicates (e.g. "foo 2.py", "bar 3.txt")
* [0-9].*

# ─── One-shot scaffolding scripts (must live in tools/ or a separate repo) ───
/*.py
/backend/*.py
/backend/maven_errors.txt
/backend/*.log

# ─── Portable runtimes (Windows) ─────────────────────────────────────────────
node-v*.zip
node-v*/
node.zip
git-portable.exe
git/

# ─── Test / coverage artifacts ────────────────────────────────────────────────
coverage/
*.lcov
.nyc_output/
# Cypress test artifacts (screenshots, videos) — generated, not source
screenshots/
frontend-host/cypress/screenshots/
frontend-host/cypress/videos/
frontend-customer/cypress/screenshots/
frontend-customer/cypress/videos/

# ─── Misc binaries ───────────────────────────────────────────────────────────
*.zip
*.tar.gz
*.tgz
*.gz
*.dmg
*.exe
*.dll
*.so
*.dylib

# ─── Load-test & runtime artifacts (not source) ──────────────────────────────
# JMeter load-test output (html_report/, *.jtl, aggregate/summary CSVs) and the
# runtime governance audit log are generated artifacts, not committed source.
/results/
/data/
*.jtl
# demo runtime (do not commit secrets/logs)
demo/.htpasswd
demo/*.log

# ─── Agent workspaces & scratch ──────────────────────────────────────────────
# Keep agent-generated handovers/plans/status reports OUT of the repo. Write
# ephemeral docs to .agent-scratch/ (gitignored) instead of the project root.
.claude/
.gemini/
.agent-scratch/
**/.agents/

